using System.Text;

namespace FairShare.Models;

public class PaymentModel 
{
    public static List<Payment> GetPayments(int userID)
    {
        List<Payment> payments = new List<Payment>();
        // using var connection = new MySqlConnection("server=localhost;database
        // connection.open();


        return payments;
    }
}